name: Black Duck SCA Scan (Bridge CLI)

on:
  push:
    branches:
      - Polaris-testing
  workflow_dispatch:

jobs:
  blackduck:
    runs-on: ubuntu-latest

    steps:
      # --------------------------------------------
      # Checkout source code
      # --------------------------------------------
      - name: Checkout Code
        uses: actions/checkout@v4

      # --------------------------------------------
      # Setup Java (needed for Detect)
      # --------------------------------------------
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # --------------------------------------------
      # Verify Maven
      # --------------------------------------------
      - name: Verify Maven
        run: mvn -version

      # --------------------------------------------
      # (Recommended for Java) Build project
      # --------------------------------------------
      - name: Build Project
        run: mvn clean install --no-transfer-progress || true

      # --------------------------------------------
      # Download Bridge CLI
      # --------------------------------------------
      - name: Download Bridge CLI
        run: |
          curl -fLsS -o bridge.zip \
            https://repo.blackduck.com/bds-integrations-release/com/blackduck/integration/bridge/binaries/bridge-cli-bundle/latest/bridge-cli-bundle-linux64.zip
          unzip -q bridge.zip
          chmod +x bridge-cli-bundle-linux64/bridge-cli

      # --------------------------------------------
      # Run Black Duck SCA ✅ (CRITICAL FIX APPLIED)
      # --------------------------------------------
      - name: Run Black Duck SCA
        run: |
          blackducksca_url=https://expressonboarding.app.blackduck.com \
          blackducksca_token=${{ secrets.BD_TOKEN }} \
          blackducksca_project_name=NodeGoat \
          blackducksca_project_version_name=Polaris-testing \
          blackducksca_scan_full=true \
          blackducksca_waitForScan=true \
          detect_search_depth=5 \
          ./bridge-cli-bundle-linux64/bridge-cli --stage blackducksca
